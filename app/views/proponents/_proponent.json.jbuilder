json.extract! proponent, :id, :name, :document, :born_in, :street, :number, :district, :city, :state, :cep, :phone, :phone2, :salary, :created_at, :updated_at
json.url proponent_url(proponent, format: :json)
